{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "framework": "astro",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/(.*)", "dest": "/$1" },
    { "handle": "filesystem" }
  ]
}